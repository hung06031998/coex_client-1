package com.upit.coex.user.contract.map;

import com.upit.coex.user.contract.base.BaseInterfaceView;
import com.upit.coex.user.contract.base.BaseInterfaceViewModel;
import com.upit.coex.user.screen.map.activity.MapActivity;

public class MapContract {
    public interface MapViewModel extends BaseInterfaceViewModel<MapActivity> {
    }

    public interface MapView extends BaseInterfaceView {

    }
}
